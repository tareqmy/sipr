<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- sipr embedded 'ooc_default' scenario: SIPp's default out-of-call     -->
<!-- responder, loaded with -oocsn ooc_default. A request whose Call-ID    -->
<!-- matches no live call spawns one call on this scenario, which answers  -->
<!-- 200 OK mirroring the request's headers and then lingers so a          -->
<!-- retransmitted request gets the 200 again:                             -->
<!--                                                                       -->
<!--   .* -> 200 -> timewait                                               -->

<scenario name="Out-of-call UAS">
  <recv request=".*" regexp_match="true" />

  <send>
    <![CDATA[
      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Length: 0

    ]]>
  </send>

  <!-- Keep the call open for a while in case the 200 is lost to be     -->
  <!-- able to retransmit it if we receive the request again.           -->
  <timewait milliseconds="4000"/>


  <!-- definition of the response time repartition table (unit is ms)   -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>

  <!-- definition of the call length repartition table (unit is ms)     -->
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
